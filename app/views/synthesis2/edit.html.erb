<div class="page-header">
  <h1 class="hand_writer_title"><%=t '.title' %></h1>
</div>

<%= render 'form', path: presentation_synthesis2_path(@presentation, @presentation.synthesis2), method: 'patch' %>
